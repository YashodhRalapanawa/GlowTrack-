<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="6dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground"
    android:stateListAnimator="@animator/card_press_animator"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="@color/surface_variant"
    app:strokeWidth="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:gravity="center_vertical">

        <!-- Enhanced water icon with animated background -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp"
            app:cardCornerRadius="16dp"
            app:cardElevation="2dp"
            app:cardBackgroundColor="@color/accent_blue_light">

            <ImageView
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:padding="12dp"
                android:src="@drawable/ic_water_drop"
                app:tint="@color/accent_blue" />

        </com.google.android.material.card.MaterialCardView>

        <!-- Intake Info with Enhanced Styling -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tv_intake_amount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/text_primary"
                android:textSize="18sp"
                android:textStyle="bold"
                android:fontFamily="sans-serif-medium"
                tools:text="250 ml" />

            <TextView
                android:id="@+id/tv_intake_time"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:textColor="@color/text_secondary"
                android:textSize="14sp"
                tools:text="2:30 PM • Today" />

        </LinearLayout>

        <!-- Enhanced date badge with better styling -->
        <com.google.android.material.chip.Chip
            android:id="@+id/chip_intake_date"
            style="@style/Widget.Material3.Chip.Assist"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Dec 15"
            android:textSize="12sp"
            android:visibility="gone"
            app:chipBackgroundColor="@color/surface"
            app:chipStrokeWidth="1dp"
            app:chipStrokeColor="@color/divider_color"
            app:chipMinHeight="32dp"
            tools:text="Dec 15"
            tools:visibility="visible" />

        <!-- Added delete button with enhanced styling -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_delete_intake"
            style="@style/Widget.Material3.Button.IconButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="8dp"
            android:contentDescription="Delete intake"
            app:icon="@drawable/ic_delete"
            app:iconTint="@color/error_red"
            app:iconSize="20dp"
           />

    </LinearLayout>

    <!-- Completion Animation Overlay -->
    <View
        android:id="@+id/completion_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_completion_overlay"
        android:alpha="0"
        android:visibility="gone" />

</com.google.android.material.card.MaterialCardView>